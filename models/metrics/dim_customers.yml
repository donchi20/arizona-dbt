semantic_model:
  - name: customers
      defaults:
        agg_time_dimension: first_order_date
      description: |
        Customer grain mart.
      model: ref('dim_customers')

      entities:
        - name: customer
          expr: customer_id
          type: primary

      dimensions:
        - name: last_name
          type: categorical